{
  "name": "actual",
  "version": "0.0.1",
  "private": true,
  "description": "A local-first personal finance system",
  "homepage": "https://github.com/actualbudget/actual/",
  "bugs": {
    "url": "https://github.com/actualbudget/actual/issues/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/sjcrivelli/actual"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist",
    "build": "yarn workspaces foreach -pt run build",
    "start": "yarn workspace desktop-electron start",
    "lint": "eslint . --ext .ts,.tsx,.js",
    "test": "vitest run",
    "typecheck": "tsc --build --force",
    "e2e": "yarn workspace desktop-electron e2e"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^24.8.1",
    "eslint": "^9.0.0",
    "prettier": "^3.2.5",
    "rimraf": "^5.0.0",
    "typescript": "^5.3.3",
    "vitest": "^1.5.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "yarn": ">=3.0.0"
  },
  "dependencies": {
    "@reduxjs/toolkit": "^2.9.1",
    "i18next": "^25.6.0",
    "memoize-one": "^6.0.0"
  }
}
